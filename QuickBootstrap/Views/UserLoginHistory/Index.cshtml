@model QuickBootstrap.Services.Util.PagedResult<QuickBootstrap.Services.Models.UserLoginHistoryPageItem>
@{
    ViewBag.Title = "登录日志";
}

<div class="row">

    <div class="col-md-2">
        @Html.Partial("Shared/_QuickMenu")
    </div>

    <div class="col-md-10 ">

        <ol class="breadcrumb">
            <li><a href="/">管理后台</a></li>
            <li><a href="/UserLoginHistory">登录日志</a></li>
            <li class="active">
                <label class="label label-success">共查询到 @Model.SizeCount 条信息</label>
            </li>
        </ol>

        <div class="callout callout-primary">
            <div class="btn-group">
                <button type="submit" class="btn btn-default btn-group-lg" name="Event" value="Query">查询</button>
            </div>
        </div>

        <table class="table table-hover table-striped">
            <thead>
                <tr>
                    <th>序号</th>
                    <th>操作时间</th>
                    <th>用户账号</th>
                    <th>访客IP</th>
                    <th>操作内容</th>
                </tr>
            </thead>
            <tbody>
                @for (var i = 0; i < Model.Result.Count; i++)
                {
                    <tr>
                        <td>@(i + 1)</td>
                        <td>@Model.Result[i].CreateTime</td>
                        <td>@Model.Result[i].UserName</td>
                        <td>@Model.Result[i].IpAddress</td>
                        <td>@Model.Result[i].Remark</td>
                    </tr>
                }
            </tbody>
        </table>

        @Html.Partial("_PartialPagingInfo")

    </div>
</div>

